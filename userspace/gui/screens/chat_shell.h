#ifndef __GUI_CHAT_SHELL_H
#define __GUI_CHAT_SHELL_H

#include "lvgl/lvgl.h"

/* The Chat app's root: a tab bar and whichever tab is showing. */
void chat_shell_create(lv_obj_t *screen, void *arg);

#endif
